import AdminSection from "@/app/admin/_components/AdminSection";

export default function AdminPlanPage() {
  return (
    <AdminSection
      mode="plan"
      title="Plan Management"
      description="Atur paket member (free, pro, business)."
    />
  );
}
